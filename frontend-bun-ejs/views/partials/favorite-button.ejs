<%
  const rawSuffix = typeof idSuffix === "string" ? idSuffix.trim() : "";
  const safeSuffix = rawSuffix.replace(/[^a-zA-Z0-9_-]/g, "").slice(0, 40);
  const suffixSegment = safeSuffix.length > 0 ? `-${safeSuffix}` : "";
  const suffixQuery = safeSuffix.length > 0 ? `?idSuffix=${encodeURIComponent(safeSuffix)}` : "";
%>
<span id="fav-<%= linkId %><%= suffixSegment %>">
  <button
    hx-post="/links/<%= linkId %>/favorite<%= suffixQuery %>"
    hx-target="#fav-<%= linkId %><%= suffixSegment %>"
    hx-swap="outerHTML"
    hx-trigger="click"
    hx-indicator="#fav-loading-<%= linkId %><%= suffixSegment %>"
    aria-label="<%= favorited ? 'Quitar guardado' : 'Guardar' %>"
    class="inline-flex items-center gap-1 rounded-md border px-1.5 py-0.5 text-xs font-medium transition-colors
           <%= favorited ? 'border-accent-secondary/50 bg-accent-secondary/15 text-accent-secondary hover:bg-accent-secondary/20' : 'border-border/70 bg-slate-900/40 text-text-secondary/90 hover:border-accent-primary/40 hover:text-text-primary' %>
           focus:outline-none focus:ring-1 focus:ring-accent-primary focus:ring-offset-1"
  >
    <% if (favorited) { %>
      <svg class="h-3.5 w-3.5" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
        <path d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z" />
      </svg>
    <% } else { %>
      <svg class="h-3.5 w-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z" />
      </svg>
    <% } %>
    <span><%= count %></span>
    <span id="fav-loading-<%= linkId %><%= suffixSegment %>" class="htmx-indicator inline-flex items-center text-[10px] text-text-secondary/70" aria-hidden="true">...</span>
  </button>
</span>
